{
  "_comment": "Store MCP credentials here. Use environment variables for sensitive data.",
  "github": {
    "token": "${GITHUB_TOKEN}"
  },
  "openai": {
    "api_key": "${OPENAI_API_KEY}"
  },
  "anthropic": {
    "api_key": "${ANTHROPIC_API_KEY}"
  },
  "portainer": {
    "url": "${PORTAINER_URL}",
    "token": "${PORTAINER_TOKEN}",
    "endpoint_id": "${PORTAINER_ENDPOINT_ID}"
  },
  "docker": {
    "host": "${DOCKER_HOST}"
  }
}
